{
  timelogs(username: "%USERNAME%", last: %LAST%) {
    nodes {
      spentAt
      timeSpent
      summary
      issue {
        title
      }
      project {
        group {
          name
        }
      }
    }
  }
}
